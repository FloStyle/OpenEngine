[package]
name = "openengine-contracts"
description = "Immutable ABI shared by the Core host (Domain A) and Wasm logic (Domain B). The physical wall between the two domains."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
bytemuck.workspace = true
fixed.workspace = true
serde.workspace = true
postcard.workspace = true

[lints]
workspace = true

[package.metadata.abi]
# Enforced at runtime by brain/orchestrator.py::verify_abi_fingerprint.
current = "2"
