# Copy to .env then fill. NEVER commit .env — it holds real secrets.
# Name referenced by config/ai.json provider (ApiKey -> key_env).
DEEPSEEK_API_KEY=
# OpenAI-compatible alternative.
OPENAI_API_KEY=
# Local llama.cpp / unsloth: key only if the server requires auth (unsloth).
UNSLOTH_API_KEY=
