[package]
name = "openengine-editor"
description = "Domain A: egui-based editor UI, run as a system within the ECS."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
openengine-contracts = { workspace = true }
openengine-ecs = { workspace = true }
openengine-math = { workspace = true }
bytemuck.workspace = true
glam.workspace = true

[features]
# The egui/gpu shell is deferred until wgpu/egui-wgpu versions align.
default = []

[lints]
workspace = true
